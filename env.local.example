# Local Development Environment Configuration
# Copy this file to .env.local for local FTP server testing

# ===========================================
# DATABASE CONFIGURATION (Local)
# ===========================================
# Use your existing database configuration
DB_HOST=192.168.1.30
DB_PORT=1433
DB_USER=sa
DB_PASS=su_password_aqui
DB_NAME=VsolDatos

# ===========================================
# APPLICATION CONFIGURATION
# ===========================================
NODE_ENV=development
PORT=3001

# URL base de la aplicación (local development)
APP_URL=http://localhost:3001

# ===========================================
# LOCAL FTP SERVER CONFIGURATION
# ===========================================
# Local FTP server (Docker container)
FTP_HOST=localhost
FTP_PORT=21
FTP_USER=usermw
FTP_PASSWORD=usermw
FTP_BASE_PATH=/uploads/orders
FTP_SECURE=false

# HTTP access to FTP files (via nginx)
FTP_HTTP_BASE_URL=http://localhost:8080

# ===========================================
# DEVELOPMENT NOTES
# ===========================================
# 1. Start local FTP server: ./scripts/setup-local-ftp.sh
# 2. FTP server will be available at localhost:21
# 3. HTTP access to files at http://localhost:8080
# 4. FTP data directory: ./ftp-data/uploads/orders/
# 5. Stop FTP server: docker-compose -f docker-compose.ftp.yml down
